import ProfileClient from "./ProfileClient";

export default function OwnProfilePage() {
    // Renders the Profile Client without passing a userId, indicating "me" mode.
    return <ProfileClient />;
}
